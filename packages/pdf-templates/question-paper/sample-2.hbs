<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>

    <!-- Inter Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

    <!-- MathJax Configuration -->
    <script id="mathjax-config">
        window.MathJax = {
            tex: {
                inlineMath: [['$', '$'], ['\\(', '\\)']],
                displayMath: [['$$', '$$'], ['\\[', '\\]']],
                packages: { '[+]': ['noerrors', 'noundefined', 'mhchem', 'physics'] }
            },
            chtml: {
                fontURL: 'https://cdn.jsdelivr.net/npm/mathjax@3/es5/output/chtml/fonts/tex',
                matchFontHeight: true,
                scale: 1,               // adjust global size if required
                mtextInheritFont: true  // make text elements use page font if available
            },
            options: {
                processHtmlClass: 'mathjax',
                enableMenu: false
            }
        };
    </script>
    <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml.js"></script>

    <style>
        /* ---------- PAGE + PRINT ---------- */
        @page {
            size: A4;
            margin: 10mm 10mm 16mm 10mm;
            counter-increment: page;
        }

        html,
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: 'Inter', sans-serif;
            font-size: 11px;
            counter-reset: page;
            background: #e5e7eb;
        }

        * {
            font-family: inherit;
        }

        /* Ensure consistent fonts for all text elements */
        .q-content p,
        .q-content span,
        .q-content div,
        .q-content li,
        .option-item p,
        .option-item span,
        .option-item div {
            font-family: 'Inter', sans-serif !important;
            font-size: 11px !important;
        }

        /* Header fonts use Inter as well */
        h1,
        h2,
        h3,
        h4,
        h5,
        h6 {
            font-family: 'Inter', sans-serif;
        }

        @media print {
            .no-print {
                display: none;
            }

            body {
                background: white;
            }

            .page {
                box-shadow: none;
                margin: 0;
                border-radius: 0;
            }
        }

        /* ---------- OUTER PAPER SHEET ---------- */
        .sheet {
            width: 210mm;
            min-height: 297mm;
            margin: 0 auto;
            padding: 0;
            background: white;
            border: 3px solid #000;
            position: relative;
            box-sizing: border-box;
        }

        .page {
            width: 210mm;
            min-height: 297mm;
            margin: 0 auto;
            background: white;
            border: 1.5px solid #000000;
            box-shadow: 0 0 6px rgba(15, 23, 42, 0.25);
            position: relative;
            box-sizing: border-box;
        }

        .main-content {
            padding-bottom: 32px;
            position: relative;
            z-index: 10;
            width: 100%;
            max-height: 100%;
            box-sizing: border-box;
            overflow: hidden;
        }

        /* Override Tailwind min-h-screen - allow content to flow */
        .min-h-screen {
            min-height: 297mm !important;
            height: auto !important;
        }

        .watermark-bg {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-30deg);
            font-size: 140px;
            font-weight: 900;
            color: rgba(0, 0, 0, 0.06);
            white-space: nowrap;
            z-index: -1;
            user-select: none;
            pointer-events: none;
        }


        /* ---------- QUESTIONS LAYOUT ---------- */
        .question-item {
            page-break-inside: avoid;
            break-inside: avoid;
            margin-bottom: 16px;
            font-size: 11px;
            line-height: 1.5;
            font-family: 'Inter', sans-serif;
            display: inline-block;
            width: 100%;
        }

        .question-grid {
            column-count: 2;
            column-gap: 20px;
            column-rule: 1px solid #e5e7eb;
            column-fill: auto;
            orphans: 2;
            widows: 2;
            height: auto;
            width: 100%;
            box-sizing: border-box;
        }

        /* Ensure questions container fits within page */
        .questions-container {
            width: 100%;
            max-width: 100%;
            box-sizing: border-box;
            padding: 0;
        }

        .question-grid .question-item {
            display: inline-block;
            width: 100%;
            margin-bottom: 16px;
            break-inside: avoid;
            page-break-inside: avoid;
            -webkit-column-break-inside: avoid;
            column-break-inside: avoid;
        }

        /* Prevent empty spaces in columns */
        .question-grid::after {
            content: "";
            display: block;
            clear: both;
        }

        .q-number {
            font-weight: bold;
            display: inline;
            margin-right: 6px;
        }

        .q-content {
            display: inline;
            line-height: 1.6;
            font-family: 'Inter', sans-serif;
            font-size: 11px;
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        .q-content img {
            max-width: 160px !important;
            max-height: 100px !important;
            width: auto !important;
            height: auto !important;
            display: block !important;
            margin: 4px auto !important;
            page-break-inside: avoid;
            object-fit: contain !important;
            break-inside: avoid;
        }

        /* Ensure images don't break column layout - more constrained */
        .question-grid .question-item img {
            max-width: 140px !important;
            max-height: 90px !important;
        }

        /* Prevent images from being too large in options */
        .options img {
            max-width: 120px !important;
            max-height: 80px !important;
        }

        .q-content p {
            margin: 3px 0;
            line-height: 1.5;
            font-size: 11px;
            font-family: 'Inter', sans-serif;
        }

        .q-content strong {
            font-weight: bold;
        }

        .q-content em {
            font-style: italic;
        }

        .q-content code {
            background-color: #f3f4f6;
            padding: 2px 4px;
            border-radius: 3px;
            font-size: 10px;
            font-family: "Courier New", Courier, monospace;
        }

        .q-content pre {
            background-color: #f3f4f6;
            padding: 8px;
            border-radius: 4px;
            overflow-x: auto;
            margin: 8px 0;
            font-size: 10px;
        }

        .q-content ul,
        .q-content ol {
            margin: 4px 0;
            padding-left: 20px;
        }

        .q-content li {
            margin: 2px 0;
        }

        .options {
            margin-top: 6px;
            margin-left: 18px;
        }

        .option-item {
            margin-bottom: 3px;
            line-height: 1.5;
            font-family: 'Inter', sans-serif;
            font-size: 11px;
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        .option-item img {
            max-width: 120px !important;
            max-height: 80px !important;
            width: auto !important;
            height: auto !important;
            display: block !important;
            margin: 3px auto !important;
            page-break-inside: avoid;
            object-fit: contain !important;
        }

        .option-item p {
            margin: 2px 0;
            line-height: 1.5;
        }

        .option-item strong {
            font-weight: bold;
        }

        .option-item em {
            font-style: italic;
        }

        .option-item code {
            background-color: #f3f4f6;
            padding: 1px 3px;
            border-radius: 2px;
            font-size: 9px;
            font-family: "Courier New", Courier, monospace;
        }

        .section-header {
            font-weight: bold;
            font-size: 11px;
            margin: 10px 0 8px;
            text-transform: uppercase;
            letter-spacing: 0.06em;
            padding: 4px 8px;
            background: #e5e7eb;
            border-left: 3px solid #000000;
            break-after: avoid;
        }

        /* ---------- FOOTER (PAGE NUMBER) ---------- */
        .print-footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            font-size: 9px;
            text-align: center;
            padding: 4px 0;
            border-top: 1px solid #d1d5db;
            background: white;
        }

        .print-footer .page-number::after {
            content: counter(page);
        }

        /* ---------- MATHJAX MATH RENDERING (Inter font, normal weight) ---------- */
        mjx-container,
        mjx-container *,
        mjx-math,
        mjx-text {
            font-family: 'Inter', sans-serif !important;
            font-weight: 400 !important;
        }

        mjx-container[jax="CHTML"][display="inline"] {
            font-size: 0.95em !important;
        }

        mjx-container[jax="CHTML"][display="true"] {
            font-size: 1.05em !important;
        }

        mjx-bold {
            font-weight: 600 !important;
        }
    </style>
</head>

<body>
    <div class="sheet">

        <!-- Watermark (text comes from Handlebars data) -->
        <div class="watermark-bg">
            {{#if watermarkText}}
            {{watermarkText}}
            {{else}}
            RANKMARG
            {{/if}}
        </div>

        <div class="min-h-screen main-content">
            <div class="bg-white border-b-4 border-black px-6 pt-4 pb-3">

                <!-- Header -->
                <div class="flex justify-between items-start">

                    <!-- LEFT LOGO / BRAND -->
                    <div class="flex items-center gap-3">
                        <div class="flex flex-col">
                            <div class="flex items-center gap-2">
                                <div class="w-16 h-10 border-2 border-black flex items-center justify-center">
                                    <span class="font-extrabold text-lg tracking-tight">ALLEN</span>
                                </div>
                                <div class="text-[10px] leading-tight font-semibold">
                                    <p>CAREER INSTITUTE</p>
                                    <p class="italic">KOTA (RAJ.)</p>
                                </div>
                            </div>
                            <p class="text-[10px] mt-1 font-medium uppercase tracking-wide">
                                Leaders in JEE (Main + Adv.) | NEET (UG) | Olympiads
                            </p>
                        </div>
                    </div>

                    <!-- CENTER INFO -->
                    <div class="text-center px-4">
                        {{#if examCode}}
                        <p class="text-[11px] font-bold uppercase tracking-wide">
                            {{examCode}}
                        </p>
                        {{/if}}

                        {{#if testCategory}}
                        <p class="text-[11px] font-semibold uppercase tracking-wide">
                            {{testCategory}}
                        </p>
                        {{/if}}

                        {{#if testDate}}
                        <p class="text-[10px] mt-1">
                            Date:
                            <span class="font-semibold">{{testDate}}</span>
                        </p>
                        {{/if}}

                        {{#if duration}}
                        <p class="text-[10px]">
                            Duration:
                            <span class="font-semibold">{{duration}}</span>
                        </p>
                        {{/if}}
                    </div>

                    <!-- RIGHT SIDE (TEST ID / LOGO) -->
                    <div class="w-20 text-right">
                        {{#if testId}}
                        <div class="font-bold text-sm">{{testId}}</div>
                        {{/if}}
                    </div>
                </div>

                <!-- MAIN TITLE -->
                <div class="text-center mt-3">
                    <h1 class="text-xl font-extrabold uppercase tracking-wide">
                        {{title}}
                    </h1>
                    {{#if description}}
                    <p class="text-xs text-gray-700 mt-1">
                        {{description}}
                    </p>
                    {{/if}}
                </div>
            </div>

            <!-- QUESTIONS -->
            <div class="px-6 py-4 questions-container">
                <div class="question-grid">

                    {{#each testSection}}
                    {{#if name}}
                    <div class="col-span-2 section-header">
                        {{name}}
                    </div>
                    {{/if}}

                    {{#each testQuestion}}
                    {{#with question}}
                    <div class="question-item">
                        <span class="q-number">Q{{inc @index}}.</span>
                        <div class="q-content">
                            {{{content}}}
                        </div>

                        {{#if options}}
                        <div class="options">
                            {{#each options}}
                            <div class="option-item">
                                ({{optLabel @index}}) {{{content}}}
                            </div>
                            {{/each}}
                        </div>
                        {{/if}}
                    </div>
                    {{/with}}
                    {{/each}}
                    {{/each}}

                </div>
            </div>
        </div>


    </div>
</body>

</html>